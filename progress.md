# Progress

## 2026-06-06

- Created production-readiness goal for smart-cs-agent.
- Started PR1: deployable sandbox hardening.
- Created persistent planning files: `task_plan.md`, `findings.md`, `progress.md`.
- Dispatched backend readiness, CI/Ops, and frontend readiness workers.
- Integrated backend readiness, GitHub Actions CI, production readiness docs, and frontend API disconnected state.
- Unified WebSocket CORS configuration through the API config loader.
- Changed Prisma startup behavior to lazy DB connection so liveness and readiness remain separate.
- Fixed final review findings by splitting `loadWebOrigin()` from full API config and constraining `.env` loading in CI/production.
- Final PR1 verification passed: db generate, API tests, typecheck, lint, build, smoke syntax, browser layout checks, and unreachable API smoke error path.
- Started PR2 tenant context baseline.
- Added request context parsing and tenant filtering for cases/rules APIs.
- Updated frontend API client and smoke script to send explicit demo tenant/operator headers.
- Passed tenant context through WeCom event handling into Agent classification/risk rules.
- Final PR2 verification passed: API tests, typecheck, lint, db generate, build, smoke syntax, unreachable API smoke error path, and browser layout checks.
- Started PR3 operator API key guard.
- Added `OPERATOR_API_KEYS` parsing, Bearer/`x-api-key` support, tenant mismatch blocking, and production default rejection for insecure operator headers.
- Updated Web and smoke script to send the sandbox operator key when configured.
- Updated demo and production-readiness docs to prefer Bearer key access for operator-facing APIs.
- Extended PR3 guard coverage to legacy `/v2/*` operator endpoints and `/v1/wecom/webhook/send`.
- Added a runtime gate for `/v1/wecom/events` through `WECOM_SANDBOX_ENABLED`.
- Changed insecure operator header fallback to default `false` in config and `.env.example`.
- Started PR4 public API surface lockdown.
- Disabled legacy Web demo APIs `/api/chat` and `/api/db` by default behind `ENABLE_LEGACY_WEB_DEMO_API`.
- Added Web route tests and a deployment API surface inventory.
- Started PR5 server-side operator BFF.
- Added `/api/operator/cases`, `/api/operator/cases/:id`, and `/api/operator/readiness` Web BFF routes.
- Updated the operator workbench client to call same-origin BFF routes instead of direct API URLs with browser-public keys.
- Tightened the mobile workbench layout back to a single viewport with internal scrolling after the BFF fallback path exposed page-level scroll.
- Started PR6 operator session boundary.
- Added signed HttpOnly operator sessions, `/api/operator/login`, and `/api/operator/logout`.
- Changed Web BFF case list/detail routes to require a session and derive API key, tenant ID, and operator ID from server-side account config.
- Added a Web login state so unauthenticated operators do not see fallback demo cases.
- Addressed final review findings: production rejects placeholder/short session secrets and default demo accounts, login rejects malformed JSON payloads with 400, production BFF fails closed without `API_URL`, and Web no longer shows offline demo cases unless explicitly enabled.
- Started PR7 operator identity/RBAC baseline.
- Added `/api/operator/me` tests and implementation for sanitized current-operator profiles.
- Added role-derived permissions to login/current-session responses and updated the workbench to show the current operator and disable confirm/takeover for read-only accounts.
- Started PR8 operator account hardening.
- Added password hash login coverage, production plaintext password rejection, disabled account rejection, and session-version invalidation tests.
- Implemented `scrypt:<salt>:<hash>` password verification, `disabled` account enforcement, and `sessionVersion` session revocation in the Web BFF session layer.
- Started and completed PR13 real-channel intake security.
- Added `POST /v1/channels/:channel/webhook/events` as a security-only webhook receipt endpoint for real channels.
- Added raw-body HMAC verification, timestamp freshness checks, tenant/channel/event ID validation, replay protection through `ChannelWebhookReceipt`, and channel readiness reporting.
- Added real-channel smoke documentation and `npm run demo:real-channel-smoke`.
- Final PR13 verification passed: Prisma generate, API tests, Web tests, typecheck, lint, build, sandbox smoke, real-channel smoke, and demo script syntax checks.
- Started PR14 real-channel payload normalization.
- Added Taobao/Douyin-shaped payload normalizer tests and implementation.
- Updated the real-channel controller to return `sandbox_queued` / `normalized_only` after writing `NormalizedChannelEvent`, while still avoiding AgentService, ActionService, case creation, messages, and customer-visible replies.
- Started PR15 real-channel review replay pool.
- Added review lifecycle fields to `NormalizedChannelEvent`.
- Added operator-gated channel event review service/controller tests and implementation for list, ignore, and manual replay.
- Addressed PR15 spec review findings by restricting the review pool to `source=real_channel_webhook`, adding a review status enum migration, and covering malformed/viewer/source boundary cases in tests.
- Final PR15 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, and real-channel replay smoke.
- Started PR16 operator workbench review pool UI.
- Added Web BFF tests and routes for `GET /api/operator/channel-events`, `POST /api/operator/channel-events/:id/replay`, and `POST /api/operator/channel-events/:id/ignore`.
- Extended the Web operator API client with sanitized pending channel event list, replay, and ignore helpers.
- Added BFF viewer-role blocking for channel event replay/ignore even when a session account is backed by a stronger server API key.
- Added the "待接入消息" workbench queue and verified in browser that pending messages can generate an internal human-reviewed case.
- Tightened `GET /api/operator/channel-events` so the browser receives only UI-safe fields and not tenant or external conversation/message identifiers.
- Final PR16 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, real-channel replay smoke, and browser checks at 1366x768 plus 390x844 with no page-level overflow or internal terminology visible.
- Started PR17 channel event replay atomic claim safety.
- Added a `processing` review status so real-channel replay first claims a pending event before creating an internal after-sales case.
- Updated replay and ignore flows to return 409 Conflict for already reviewed events instead of treating them as missing or allowing duplicate replay work.
- Final PR17 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, and real-channel replay smoke.
- Started PR18 channel event processing recovery.
- Added an admin-only API and Web BFF recovery path for stale real-channel events stuck in `processing`.
- Recovery moves stale claims back to `pending`, clears the processing claim fields, and writes an audit log without calling AgentService or sending customer-visible replies.
- Final PR18 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, real-channel replay smoke, and a live recovery endpoint smoke returning `recoveredCount: 0`.
- Started PR19 channel event queue metrics.
- Added tenant-scoped API and Web BFF metrics for pending, processing, stale processing, replayed, ignored, and oldest pending age.
- BFF metrics responses strip tenant/source/internal fields before returning to the browser.
- Final PR19 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, real-channel replay smoke, and a live metrics endpoint smoke returning queue counts and oldest pending age.
- Started PR20 channel queue readiness thresholds.
- Connected aggregate real-channel queue health into `/health/ready`, returning `degraded` when configured thresholds are exceeded while keeping DB failure as `unhealthy`/503.
- Added queue threshold env vars to `.env.example` with blank warning thresholds disabled by default.
- Final PR20 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, real-channel replay smoke, and a live degraded readiness smoke with aggregate queue reasons only.
- Started PR21 channel queue operations runbook.
- Added `docs/deploy/channel-queue-runbook.md` to document readiness states, queue metrics, stale recovery steps, degraded reason codes, and safety boundaries.
- Added `scripts/verify-channel-queue-runbook.mjs` plus `npm run verify:channel-runbook` so the runbook, env example, public API surface, and source routes are checked together.
- Final PR21 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, WeCom sandbox smoke, real-channel normalization smoke, and real-channel replay smoke.
- Started PR22 operator queue operations status.
- Extended the Web API client to preserve degraded readiness queue details, fetch sanitized channel queue metrics, and call admin stale recovery through the BFF.
- Added a compact operator workbench queue status panel showing pending intake, stuck processing claims, oldest wait time, and admin recovery for stuck messages.
- Final PR22 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, browser checks at 1366x768 and 390x844 with no page-level overflow or internal terms, WeCom sandbox smoke, real-channel normalization smoke, and real-channel replay smoke.
- Started PR23 queue operation audit records.
- Added recovery audit enrichment so stale processing recovery records include recovery cutoff, recovered count, queue-after counts, and whether stale processing claims were cleared.
- Added tenant-scoped `GET /v1/channel-events/operation-audits` and admin-only `GET /api/operator/channel-events/operation-audits` with sanitized response mapping.
- Extended the Web API client and operator workbench queue panel to show compact admin-only recent recovery records without raw audit details.
- Final PR23 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, live WeCom sandbox smoke, live real-channel normalization smoke, live real-channel replay smoke, live operation-audits endpoint check, browser checks at 1366x768 and 390x844 with no page-level overflow or internal terms, and independent code review with no blocking findings.
- Started PR24 queue audit summary.
- Added tenant-scoped admin `GET /v1/channel-events/audit-summary` coverage for bounded queue review totals and per-operator aggregation.
- Added admin-only `GET /api/operator/channel-events/audit-summary` BFF route with a sanitized response whitelist and query-window passthrough.
- Extended the Web API client and operator workbench queue panel to show compact admin-only queue handling totals without raw audit details.
- Addressed independent review findings by rejecting inverted/future-only audit windows, capping custom audit windows at 24 hours, making the BFF fail closed on malformed numeric summary fields, and renaming the UI summary label to "近24小时".
- Final PR24 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, live WeCom sandbox smoke, live real-channel normalization smoke, live real-channel replay smoke, live audit-summary endpoint checks for success/future-only/wide-window cases, and live BFF audit-summary check. Browser layout checks passed before the label-only change; the later browser plugin session timed out during re-check, so the label change is covered by source/build rather than a fresh browser snapshot.
- Started PR25 real-channel webhook rate limit.
- Added `REAL_CHANNEL_WEBHOOK_RATE_LIMIT_PER_MINUTE` config parsing with a default of `0` and validation that rejects negative values.
- Added `RealChannelRateLimitService` and wired it into signed real-channel webhook intake after HMAC verification and before persistence.
- Added rate-limit tests covering disabled mode, per `channel:tenantId` isolation, next-minute reset, and controller-level 429 behavior that avoids extra receipt or normalized-event writes.
- Updated `.env.example`, channel queue runbook, public API surface, production-readiness notes, and runbook verifier for the new 429 safety boundary.
- Addressed independent review findings by switching the limiter key to structured `JSON.stringify([channel, tenantId])`, pruning old minute buckets, and covering invalid-signature requests that must not spend valid quota.
- Final PR25 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, and live real-channel rate-limit smoke on port 4101 showing first signed webhook 202, second signed webhook 429, one receipt, and one normalized event.
- Started PR26 production real-channel intake gates.
- Added fail-closed API config validation for `NODE_ENV=production` plus `REAL_CHANNEL_WEBHOOKS_ENABLED=true`.
- Production real-channel intake now requires at least one configured webhook secret, a positive per-minute application rate limit, an explicit freshness window, and all channel queue thresholds before API startup can proceed.
- Updated production readiness docs, channel queue runbook, public API surface, sandbox CI example, and the runbook verifier for the PR26 intake-gate contract.
- Addressed independent review coverage feedback by making the config spec table-driven for each required production intake gate, including `REAL_CHANNEL_WEBHOOK_RATE_LIMIT_PER_MINUTE=0` and each queue threshold.
- Reduced secret propagation risk by keeping `REAL_CHANNEL_WEBHOOK_SECRETS` out of the returned `ApiConfig` object; disabled real-channel intake can still start even if a stale secret env value is malformed.
- Final PR26 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, and a built-config runtime check proving missing production intake gates are rejected, fully configured production intake is accepted, disabled intake does not parse stale secrets, and webhook secrets are not propagated through `ApiConfig`.
- Started PR27 production readiness verifier.
- Added `npm run verify:production-readiness`, backed by `scripts/verify-production-readiness.mjs`.
- The verifier supports `--env-file`, `--require-real-channel`, `--api`, and `--allow-degraded`, and checks production env shape, dangerous sandbox/demo toggles, operator identity/account boundaries, placeholder secrets, operator API key strength, conditional real-channel intake gates, and optional live `/health/ready`.
- Updated production-readiness docs and planning files to make the verifier part of the launch gate.
- Addressed independent review findings by redacting unknown CLI argument values, validating `OPERATOR_API_KEYS[*].role`, and requiring `checks.channelWebhooks.status=ok` when `--require-real-channel` is combined with live `--api` readiness.
- Final PR27 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, production-readiness verifier bad-env/good-env checks, unknown-argument redaction check, and live `/health/ready` checks for both success and channel-webhook subcheck failure.
- Started PR28 real-channel gray-release allowlist.
- Added `REAL_CHANNEL_WEBHOOK_ALLOWLIST` as an exact `channel + tenantId` gate after signature verification and before rate limiting or persistence.
- Updated production startup gates, production readiness verifier, readiness schema, docs, smoke instructions, and runbook verifier so real-channel intake cannot be opened without an explicit allowlisted pair.
- Final PR28 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, smoke script syntax checks, production-readiness verifier missing-allowlist failure, fully allowlisted production env success, and independent code review with no findings.
- Started PR29 production operator identity closure.
- Blocked env-backed operator identity provider and env account source at Web BFF runtime when `NODE_ENV=production`.
- Added `npm run operator:bootstrap-admin` and `npm run verify:operator-bootstrap` so deployments can create the first database-backed admin without printing passwords, API keys, or password hashes.
- Addressed independent review by also rejecting `OPERATOR_ACCOUNT_SOURCE=env` when `OPERATOR_IDENTITY_PROVIDER=database` is set in production, and by avoiding secret-marker echo in verifier failure messages.
- Final PR29 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, operator bootstrap verifier, script syntax checks, production-readiness env-account failure/good-env success checks, and independent code review follow-up fixes.
- Started PR30 real-channel emergency kill switch.
- Added `REAL_CHANNEL_WEBHOOK_KILL_SWITCH` coverage so signed real-channel intake fails closed with HTTP 503 before signature work, application rate limiting, replay receipt writes, normalized event writes, case creation, action execution, or customer-visible replies.
- Updated readiness, shared schema, config parsing, production-readiness verifier, env examples, channel queue runbook, public API surface, and production-readiness docs for `disabled_by_kill_switch`.
- Addressed independent review by making the kill switch take precedence over `REAL_CHANNEL_WEBHOOKS_ENABLED`, so intake, readiness, and production warnings consistently show the emergency stop state even if the normal intake flag is off.
- Final PR30 verification passed: Prisma generate, Prisma migrate deploy, API tests, Web tests, full typecheck, lint, production build, runbook verifier, operator bootstrap verifier, script syntax checks, production-readiness kill-switch pass/fail/warning scenarios, and independent code review follow-up fixes.
- Started PR31 public monitoring metrics.
- Added a public `GET /metrics` API endpoint that returns Prometheus text for API up, database readiness, real-channel webhook readiness, kill-switch state, source-wide queue counts, oldest pending age, and known degraded reasons without tenant/channel/customer/payload/secret labels.
- Added DB-down metrics behavior so monitoring can still scrape `smart_cs_agent_api_up 1` and `smart_cs_agent_database_ready 0` without exposing the database error.
- Addressed independent review by adding PR31 production-readiness text checks to `npm run verify:channel-runbook`.
- Final PR31 verification passed: API tests, Web tests, full typecheck, lint, production build, Prisma generate/migrate deploy, runbook verifier, operator bootstrap verifier, script syntax checks, production-readiness pass/fail/warning scenarios, and independent code review follow-up.
- Started PR32 production canary verifier.
- Added `scripts/verify-production-canary.mjs` and `npm run verify:production-canary` to check live public `/health/ready` plus `/metrics` after deploy.
- Added `scripts/verify-production-canary.test.mjs` with local HTTP canary coverage for healthy deploys, degraded queue handling, required real-channel intake, kill-switch failure, sensitive metric marker detection, and unknown-argument redaction.
- Updated production readiness and queue runbook docs, plus the runbook verifier, so PR32 canary behavior and safety boundaries are guarded.
- Fixed the API workspace test script to use a small Node runner that sets `TSX_TSCONFIG=tsconfig.json` and invokes the `tsx` CLI directly, so decorator-based controller specs run reliably under the current Node/tsx toolchain.
- Addressed independent review findings by redacting bare unknown URL-like arguments and making `/metrics` leakage detection fail on non-public label keys such as tenant/channel/secret labels.
- Final PR32 verification passed: canary behavior tests, canary package command live check, API tests, Web tests, typecheck, lint, production build, Prisma generate/migrate deploy, runbook verifier, operator bootstrap verifier, production-readiness good/bad scenarios, and script syntax checks.
- Started PR33 production alerting pack.
- Added `docs/deploy/production-alerting.md`, `docs/deploy/production-alerts.prometheus.yml.example`, and `docs/deploy/production-canary-schedule.yml.example`.
- Added `npm run verify:production-alerting` to guard alert rule coverage, scheduled canary parameters, and no-secret/no-tenant alerting boundaries.
- Connected PR33 alerting docs into production-readiness notes and the channel queue runbook verifier.
- Addressed independent review findings by changing the API-down Prometheus alert to use scrape health/absent metrics and broadening alerting verifier sensitive-marker checks across naming styles.
- Addressed follow-up independent review findings by scoping the API-down `absent()` expression to `job="smart-cs-agent"`, upgrading the alerting verifier to validate enabled alert blocks, requiring the scheduled canary to use `--api="$SMART_CS_API_URL"`, and replacing concrete-looking production doc examples with placeholders.
- Final PR33 verification passed: production-alerting verifier, channel runbook verifier, script syntax checks, production canary tests/checks, operator bootstrap verifier, Prisma generate/migrate deploy, API tests, Web tests, full typecheck, lint, production build, and independent follow-up review with no findings.
- Started PR34 production launch and rollback runbook.
- Added `docs/deploy/production-launch-runbook.md` to connect launch decision gates, preflight commands, deploy sequence, rollback triggers, kill-switch/allowlist/intake rollback actions, stale-claim recovery evidence, post-launch evidence, and rehearsal scenarios.
- Added `npm run verify:production-launch` to guard the launch runbook against drifting away from readiness, canary, alerting, queue operations, rollback controls, recovery evidence, and no-secret/no-customer-action boundaries.
- Addressed independent review findings by replacing concrete-looking smoke examples in `task_plan.md`, expanding launch verification to scan PR34-touched docs and the task plan, and explicitly tying PR34 rollback triggers to the PR33 alert names.
- Final PR34 verification passed: production-launch verifier, production-alerting verifier, channel runbook verifier, operator bootstrap verifier, production canary tests, script syntax checks, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, production build, and independent follow-up review with no Critical/Important findings.
- Started PR35 provider adapter contract package.
- Added shared provider adapter mode/write-policy fields to the operator integration contract.
- Added `ProviderAdapterRegistry` so integration status and action policy come from adapter contracts instead of static operation-service data.
- Updated current Taobao and Douyin adapters to declare `sandbox_mock`, `sandbox_only`, no customer-visible actions, and no real commerce actions.
- Updated `OpsService` so `GET /v2/integrations` reads provider contracts and `POST /v2/actions/execute` blocks commerce writes through provider write policy while keeping internal `handoff` queueable.
- Added `docs/deploy/provider-adapter-contracts.md` and `npm run verify:provider-adapters` to guard the no-real-write boundary.
- Addressed PR35 review feedback by changing old sandbox `ActionService.executeMockAction` provider operations from `success` to `simulated`, adding ActionService regression tests, and extending provider verification to guard that mock path.
- Final PR35 verification passed: provider adapter verifier, production launch verifier, production alerting verifier, channel runbook verifier, operator bootstrap verifier, production canary tests, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, and script syntax checks.
- Started PR36 real provider readonly foundation.
- Added `PROVIDER_READONLY_ADAPTERS` parsing with strict `{channel, tenantId, credentialRef}` records so provider credentials are referenced through deployment secret storage instead of inlined in env JSON.
- Added shared `readCapabilities` support and projected configured channels as `real_readonly` / `read_only` with only `handoff` as an executable action.
- Added `npm run verify:provider-readonly` and connected PR36 into production readiness, public API surface, launch runbook, `.env.example`, and the task plan.
- Addressed PR36 independent review findings by constraining `credentialRef` to `secret://` or `vault://`, making readonly projection tenant-and-channel scoped, and deriving action tenant IDs from request context rather than request bodies.
- Final PR36 verification passed: Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, provider readonly/adapters verifiers, launch/alerting/channel/operator verifiers, production canary tests, script syntax checks, production readiness good-env and dangerous-env checks, `git diff --check`, and independent review with no Critical/Important/Minor findings.
- Started PR37 provider read execution contract.
- Added strict shared provider read request/response schemas for `get_order` and `query_logistics`, including exact lookup requirements and a response contract that forbids provider payload data.
- Added `POST /v2/provider-reads/execute` behind operator request context; body-supplied tenant/operator values are overwritten before policy evaluation.
- Added provider read policy evaluation for `real_readonly` / `read_only` tenant-channel pairs, while accepted reads still return `networkExecution=not_implemented` and `providerDataReturned=false`.
- Added `npm run verify:provider-read-contract` and connected PR37 into provider docs, production readiness, public API surface, launch runbook, and the task plan.
- Addressed PR37 review feedback by adding body-spoofing, no-context, no-provider-call, unsupported-capability, strict-schema, and exact-response tests.
- Final PR37 verification passed: API tests, Web tests, typecheck, lint, build, Prisma generate/migrate deploy, provider read contract/readonly/adapters verifiers, launch/alerting/channel/operator verifiers, production canary tests, script syntax checks, production readiness good-env and dangerous-env checks, `git diff --check`, and independent review with no Critical/Important/Minor findings.
- Started PR38 provider read audit and idempotency.
- Added `ProviderReadRun` persistence with tenant-scoped idempotency on `tenantId + idempotencyKey`, safe `lookupHash` / `lookupKeys` / `requestHash` metadata, and no raw order IDs, logistics IDs, provider payloads, customer data, or provider responses.
- Added sanitized provider read audit logging for new attempts and idempotency conflicts while keeping accepted readonly reads at `networkExecution=not_implemented` and `providerDataReturned=false`.
- Addressed independent review findings by verifying `caseId + authenticated tenant` before persisted provider reads, blocking cross-tenant case attachment before `ProviderReadRun` creation, and auditing that failure without a case-scoped audit log or raw lookup values.
- Added `npm run verify:provider-read-audit` and connected PR38 into provider docs, production readiness, public API surface, launch runbook, and the task plan.
- Final PR38 verification passed: Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, provider read audit/contract/readonly/adapters verifiers, launch/alerting/channel/operator verifiers, production canary tests, script syntax checks, `git diff --check`, and independent review follow-up fixes.
- Started PR39 provider read operations visibility.
- Added admin-only `GET /v2/provider-reads/runs` and `GET /v2/provider-reads/summary` for tenant-scoped, sanitized provider read run visibility.
- Added admin-only Web BFF routes at `/api/operator/provider-reads/runs` and `/api/operator/provider-reads/summary`, with response whitelisting so raw lookup values, full hashes, provider payloads/responses, customer data, tenant secrets, operator API keys, and provider tokens do not reach the browser.
- Addressed independent review findings by rejecting the legacy insecure `x-tenant-id` header fallback for direct provider read operations visibility, requiring a real admin operator API key, and widening verifier leak sentinels for provider responses, raw lookup values, operator API key fields, and customer data.
- Added `npm run verify:provider-read-operations` and connected PR39 into provider docs, production readiness, public API surface, launch runbook, and the task plan.
- Final PR39 verification passed: provider read operations verifier, production launch verifier, API tests, Web tests, typecheck, lint, build, and `git diff --check`.
- Started PR40 provider credential resolution boundary.
- Added `ProviderCredentialResolverService` as the no-secret/no-network boundary for future readonly provider credentials, returning only `status=not_implemented`, `credentialRefFingerprint`, `credentialMaterialLoaded=false`, and `secretValueReturned=false`.
- Extended `ProviderAdapterRegistry` with exact tenant/channel `credentialRef` lookup while keeping `GET /v2/integrations` free of full refs and secret manager paths.
- Wired `OpsService.executeProviderRead` so credentials resolve only after case ownership, idempotency checks, readonly policy acceptance, and successful `ProviderReadRun` creation; blocked reads, case mismatches, idempotency replays/conflicts, and raced duplicate creates do not resolve credentials.
- Added `npm run verify:provider-credential-boundary` and connected PR40 into provider docs, production readiness, public API surface, launch runbook, production launch verifier, and the task plan.
- Addressed independent review P1 by moving resolver invocation after the unique-key run create succeeds and adding the raced-idempotency regression test.
- Final PR40 verification passed: API tests, Web tests, db generate/migrate deploy, provider credential/read-only/read-contract/read-audit/read-operations/adapter verifiers, production launch verifier, typecheck, lint, build, and `git diff --check`; independent follow-up review reported no P0/P1/P2 findings.
- Started PR41 provider credential store boundary.
- Added `PROVIDER_CREDENTIALS` as a ref-only credential inventory: records may contain only `{ credentialRef }`, reject inline token material/API keys/client secrets, reject duplicates, and never flow into `loadApiConfig()`.
- Added `ProviderCredentialStoreService` and wired it into `ProviderCredentialResolverService` so provider reads can audit `configured`, `missing`, `invalid`, or `not_implemented` plus `credentialRefConfigured`, while keeping `credentialMaterialLoaded=false` and `secretValueReturned=false`.
- Kept provider read execution contract unchanged: accepted reads still return `networkExecution=not_implemented`, `providerDataReturned=false`, create only sanitized `ProviderReadRun` rows, and do not call Taobao/Douyin/provider networks.
- Added API tests for ref-only credential config, inline secret rejection, duplicate ref rejection, store states, resolver metadata, and Ops audit no-leak behavior.
- Hardened `ProviderCredentialResolverService` so malformed direct `credentialRef` inputs return `invalid` / `source=unknown` without echoing the supplied value.
- Added `npm run verify:provider-credential-store`, updated the PR40 credential boundary verifier for PR41 metadata, and connected the new verifier into production launch checks and deploy docs.
- Addressed independent security review by removing the earlier credential-material-in-env model and using a safer ref presence inventory with `credentialRefConfigured=true` instead of `credentialMaterialLoaded=true`.
- Final PR41 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, and `git diff --check`.
- Started PR42 provider readonly sandbox harness.
- Added `ProviderReadonlyClientHarnessService` as a no-network execution planning boundary for future readonly provider clients, keeping `networkExecution=not_implemented`, `networkAttempted=false`, `providerDataReturned=false`, `providerResponseCaptured=false`, and `attemptCount=0`.
- Added bounded provider read harness config with `PROVIDER_READ_TIMEOUT_MS` and `PROVIDER_READ_MAX_RETRIES`, plus API config tests for default, explicit, and invalid values.
- Wired `OpsService` to plan readonly sandbox execution only after policy acceptance, case ownership, provider read run creation, and credential resolution; the response contract remains unchanged and provider execution metadata is audit-only.
- Added `npm run verify:provider-read-harness` and connected PR42 into provider adapter docs, production readiness, public API surface, production launch runbook, `.env.example`, and the task plan.
- Addressed follow-up verification drift by making the harness execution modes explicit and narrowing the PR38 audit verifier's forbidden provider response marker so it still blocks raw provider responses without rejecting the safe `providerResponseCaptured=false` flag.
- Final PR42 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store/read-harness verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, and `git diff --check`.
- Started PR43 merchant launch preflight.
- Added `npm run verify:merchant-launch-preflight` as a local env-file preflight for one target tenant/channel pair, with optional real-channel and provider-readonly requirements.
- Added behavior tests proving the preflight passes with sanitized evidence, fails when the target pair is not allowlisted, fails when readonly credential inventory is missing the required ref, and redacts unknown argument values.
- Connected the merchant launch preflight into production readiness docs, production launch runbook, and the production launch verifier.
- Addressed independent review by adding a regression test for unknown bare sensitive arguments and redacting all unknown no-value arguments as `<redacted>`.
- Final PR43 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, merchant launch preflight behavior tests, provider readonly/credential-store/read-harness verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, `git diff --check`, and independent follow-up review with no Critical/Important/Minor findings.
- Started PR44 launch evidence bundle.
- Added `npm run generate:launch-evidence` to create a sanitized local JSON launch evidence bundle for one tenant/channel pair without API, database, vault, secret-manager, provider-network, or provider-write execution.
- Added pass/fail behavior tests for the bundle schema, merchant readiness checks, provider readonly evidence, safety boundary evidence, and unknown argument redaction.
- Added `npm run verify:launch-evidence` plus production readiness, launch runbook, production launch verifier, and task plan references so the evidence bundle remains part of the launch gate.
- Addressed independent review findings by sanitizing invalid channel values in failed bundles, refusing to overwrite existing evidence output files, hiding the raw output path from success logs, and documenting/verifying `verify:launch-evidence`.
- Final PR44 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, launch evidence behavior tests, merchant launch preflight behavior tests, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store/read-harness verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, `git diff --check`, and independent review follow-up fixes.
- Started PR45 launch evidence archive safety.
- Added safe env-mode launch commands for merchant preflight, launch evidence generation, and evidence archive verification so recorded npm command logs do not need raw tenant IDs, env-file paths, or evidence archive paths.
- Added a local launch evidence archive verifier that checks the PR44 bundle schema, required launch tracks, pass status when requested, required check names, and forbidden raw sensitive fields or values.
- Addressed independent review findings by enforcing an evidence-string key whitelist in archived bundles, adding tests for `tenantId`, `orderId`, `logisticsId`, and provider response evidence leaks, removing raw PR43/PR44 launch commands from the current gate, and documenting the required `SMARTCS_LAUNCH_*` env matrix for safe launch commands.
- Final PR45 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, launch evidence/preflight/archive behavior tests, safe env-mode command chain, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store/read-harness verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, and independent review follow-up fixes.
- Started PR46 multi-merchant launch manifest.
- Added a local `smart-cs-agent.launch-manifest.v1` verifier for multi-tenant/channel launch windows that reads only manifest and evidence archive JSON files, validates duplicate-free tenant fingerprint/channel entries, and cross-checks each referenced launch evidence archive.
- Added `npm run verify:launch-manifest` and `npm run verify:launch-manifest:safe`, documented the manifest schema and safe env vars, and connected PR46 to launch evidence and production launch static verifiers.
- Addressed independent review findings by closing manifest/archive schemas, blocking path/body fields such as env paths, evidence paths, metric bodies, and response bodies, adding typed validators for evidence values, requiring every check to pass when `--require-pass` is set, and expanding PR46 progress/task-plan static gates.
- Final PR46 verification passed: Prisma generate/migrate deploy, API tests, Web tests, production canary tests, launch manifest/archive/evidence/preflight behavior tests, safe env-mode command chain, safe manifest verification, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store/read-harness verifiers, production alerting/launch/channel-runbook/operator-bootstrap verifiers, script syntax checks, typecheck, lint, build, and `git diff --check`.
- Started PR47 production deployment artifacts.
- Added production Dockerfiles for API and Web, `.dockerignore`, a production-shaped compose example, Next standalone output, deployment artifact docs, and `npm run verify:production-deploy-artifacts` to keep image startup and safe deployment defaults connected to launch checks.
- Final PR47 verification passed: Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build with Next standalone output, production deploy artifact verifier, production launch verifier, launch evidence verifier, production alerting verifier, channel runbook verifier, provider adapter/readonly/read-contract/read-audit/read-operations/credential-boundary/credential-store/read-harness verifiers, launch manifest/archive/evidence/preflight tests, operator bootstrap verifier, demo script syntax checks, compose config parsing, and `git diff --check`.
- Started PR48 production image build gate.
- Added `npm run verify:production-image-builds`, `npm run verify:production-image-builds:docker`, `docs/deploy/production-image-builds.md`, and `docs/deploy/production-image-build.yml.example` so API/Web Docker images can be statically checked everywhere and actually built in Docker-enabled CI without publishing.
- Final PR48 verification passed for static image-build gate, image-build unit tests, production launch/deploy verifiers, provider boundary verifiers, launch evidence/manifest tests, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, and `git diff --check`. The Docker-backed image build command was not marked passed in this local environment because the Docker daemon path hit host permission/time limits; the command is covered by tests and should run in Docker-enabled CI.
- Started PR49 production container runtime smoke gate.
- Added `npm run verify:production-container-smoke`, `npm run verify:production-container-smoke:docker`, `docs/deploy/production-container-smoke.md`, and `docs/deploy/production-container-smoke.yml.example` so built API/Web containers can be statically checked everywhere and smoke-tested in Docker-enabled CI without publishing or calling real channels.
- Final PR49 verification passed for static container smoke gate, container-smoke unit tests, production launch/deploy/image/alerting verifiers, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, provider adapter/read-harness verifiers, and `git diff --check`. The Docker-backed container smoke command was attempted locally with host Docker access and failed at Web container startup, so it is not counted as passed in this environment; it should run after image build in Docker-enabled CI.
- Started PR50 production image security evidence gate.
- Added `npm run verify:production-image-security`, `npm run verify:production-image-security:docker`, `docs/deploy/production-image-security.md`, and `docs/deploy/production-image-security.yml.example` so built and smoke-tested API/Web images can produce SBOM and vulnerability evidence in Docker-enabled CI without publishing, reading deployment secrets, or calling real channels.
- Final PR50 verification passed for static image security gate, image-security unit tests, production launch/deploy/image-build/container-smoke verifiers, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, provider boundary verifiers, launch evidence/manifest tests, production canary tests, and `git diff --check`. The Docker-backed image security command was attempted locally with host Docker access and failed at Web SBOM plus API/Web vulnerability scans, so it is not counted as passed in this environment; it should run after image build and container smoke in Docker-enabled CI.
- Started PR51 production release provenance and promotion boundary.
- Added `npm run verify:production-release-provenance`, `npm run verify:production-release-provenance:safe`, `docs/deploy/production-release-provenance.md`, and `docs/deploy/production-release-provenance.yml.example` so release CI can verify sanitized image digest, signature, provenance, SBOM-attestation, prior gate, and promotion approval facts without publishing images, authenticating to a registry, reading secrets, or calling real channels.
- Final PR51 verification passed for static release provenance gate, release-provenance unit tests, production launch/deploy/image-build/container-smoke/image-security verifiers, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, provider boundary verifiers, launch evidence/manifest tests, production canary tests, channel runbook, operator bootstrap, and `git diff --check`. The safe env-mode release provenance command is covered by fixture tests; real signing, registry publish, and deployment promotion remain outside this verifier and must be handled by a separate reviewed release stage.
- Started PR52 production release evidence archive.
- Added `npm run verify:production-release-evidence`, `npm run verify:production-release-evidence:safe`, `docs/deploy/production-release-evidence.md`, and `docs/deploy/production-release-evidence.yml.example` so post-deploy launch evidence can bind release provenance, launch manifest, readiness, canary, rollback ownership, operator coverage, and safety facts without calling production services, reading secrets, publishing images, or executing real channel/provider actions.
- Addressed PR51 security review findings by restricting release provenance evidence reads to `production-release-provenance-artifacts/`, adding path-boundary regression coverage, and changing the provenance workflow artifact upload to an explicit file allowlist.
- Final PR52 verification passed for static release evidence gate, release-evidence unit tests, production launch/deploy/image-build/container-smoke/image-security/provenance verifiers, Prisma generate/migrate deploy, API tests, Web tests, typecheck, lint, build, launch evidence/manifest tests, production canary tests, provider adapter/read-harness verifiers, channel runbook, and `git diff --check`. The safe env-mode release evidence command is covered by fixture tests; real API calls, DB reads, registry publish, secret reads, provider calls, and production traffic changes remain outside this verifier.
- Started PR53 production change approval gate.
- Added `npm run verify:production-change-approval`, `npm run verify:production-change-approval:safe`, `docs/deploy/production-change-approval.md`, and `docs/deploy/production-change-approval.yml.example` so production launch windows can require sanitized approvals, freeze window, rollback owner, kill-switch readiness, rollback drill, operator coverage, communication readiness, and safety facts without calling production services, reading secrets, publishing images, or executing real channel/provider actions.
- Addressed PR53 review feedback by requiring `changeWindow.targetEnvironment=production` when pass evidence is required and by adding exact workflow artifact upload path coverage for `production-change-approval-artifacts/production-change-approval.json`.
- Final PR53 verification passed for static production change approval gate, change-approval unit tests, production launch verifier, typecheck, lint, build, and `git diff --check`. The safe env-mode change approval command is covered by fixture tests; real registry signing/publish, provider writes, customer-visible replies, secret reads, and production traffic changes remain outside this verifier.
- Started PR54 production launch binding gate.
- Added `npm run verify:production-launch-binding`, `npm run verify:production-launch-binding:safe`, `docs/deploy/production-launch-binding.md`, and `docs/deploy/production-launch-binding.yml.example` so release provenance, production release evidence, production change approval, and launch manifest artifacts must bind to the same release id, source commit, production target, change ticket, manifest scope, and artifact SHA-256 set before a production launch window can open.
- Addressed PR54 review feedback by requiring fail-closed source and change ticket fields, adding `launchManifest.scopeHash` binding for sorted manifest entries, rejecting symlinked or realpath-escaped artifact files, and adding regression tests for same-count manifest swaps, missing source/ticket, artifact basename mismatch, sensitive evidence, and symlink handling.
- Final PR54 verification passed for static production launch binding gate, launch-binding unit tests, release provenance/evidence/change approval/launch manifest adjacent tests, production launch verifier, API tests, Web tests, Prisma generate/migrate deploy, typecheck, lint, build, and `git diff --check`. The safe env-mode binding command is covered by fixture tests; real registry signing/publish, provider writes, customer-visible replies, secret reads, production traffic changes, and real provider network execution remain outside this verifier.
- Started PR55 production static CI gate.
- Added `.github/workflows/production-static-gates.yml`, `npm run verify:production-static-ci`, `docs/deploy/production-static-ci.md`, and `scripts/verify-production-static-ci.mjs` so PR/push CI runs tests, typecheck, lint, build, production static gates, and provider safety gates without secrets, production databases, live canaries, launch artifacts, Docker publish, registry login, deployment commands, real channels, provider network execution, or customer-visible actions.
- Addressed PR55 review feedback by parsing workflow `run:` commands into an exact allowlist, rejecting direct production readiness/canary scripts, real-channel smoke commands, Docker-backed `:docker` gates, ad hoc network commands, and unapproved run lines without echoing secret-like arguments.
- Final PR55 verification passed: Prisma generate, API tests, Web tests, production verifier script aggregate tests, static CI verifier tests, typecheck, lint, build, `verify:production-static-ci`, `verify:production-launch`, and `git diff --check`. The workflow remains a static PR/push/manual rerun gate only; real production DB migrations, live canary, registry publish/signing, deployment promotion, provider network execution, provider writes, real channel callbacks, and customer-visible replies remain outside PR55.
- PR55 local commit `3e402a8` could not be pushed because GitHub rejected workflow file updates without OAuth `workflow` scope; local work continues and the next successful authenticated push should include PR55 and later commits together.
- Started PR56 production branch protection gate.
- Added `npm run verify:production-branch-protection`, `npm run verify:production-branch-protection:safe`, `docs/deploy/production-branch-protection.md`, and `scripts/verify-production-branch-protection.mjs` so repository branch protection evidence can prove `Static production gates` is a required status check, pull request reviews are enforced, force pushes/deletions are disabled, bypass actors are empty, and the verifier does not call GitHub APIs or mutate settings.
- Addressed PR56 review feedback by adding branch protection safe-mode env vars to the launch runbook injection list, making the verifier check that paragraph directly, replacing case-folded path-prefix checks with `path.relative()` boundary checks, and rejecting non-ISO `generatedAt` values.
- Final PR56 verification passed: Prisma generate, API tests, Web tests, production verifier script aggregate tests, branch-protection verifier tests, typecheck, lint, build, `verify:production-branch-protection`, `verify:production-launch`, and `git diff --check`. The gate verifies local sanitized evidence only; actual GitHub branch protection settings still need to be applied by a repository administrator or an audited infrastructure workflow.
- Started PR57 production provider write approval gate.
- Added `npm run verify:production-provider-write-approval`, `npm run verify:production-provider-write-approval:safe`, `docs/deploy/production-provider-write-approval.md`, and `scripts/verify-production-provider-write-approval.mjs` so a future real provider write pilot must prove `human_review_required`, low-risk action allowlist, idempotency, audit, provider write kill switch, limits, rollback ownership, and no automatic provider writes before any implementation can execute provider network mutations.
- Addressed PR57 review feedback by making any provided approval evidence require `human_review_required` and core safety controls even without `--require-pass`, adding a regression test for that path, and resolving approval paths through realpaths so symlinks or junctions cannot escape `production-provider-write-approval-artifacts/`.
- Addressed PR57 production safety review feedback by hardening `verify:production-provider-write-approval:safe` to include `--require-pass`, requiring `approvalStatus=approved`, distinct reviewer fingerprints, artifact hash bindings, and adding the provider-write approval tests and static gate into `.github/workflows/production-static-gates.yml`.
- Final PR57 verification passed: Prisma generate, API tests, Web tests, production verifier script aggregate tests, provider-write approval verifier tests, typecheck, lint, build, `verify:production-provider-write-approval`, `verify:production-launch`, and `git diff --check`. This gate still verifies approval evidence only; actual provider write clients, persisted write-run execution, live kill-switch enforcement before network calls, and real customer-visible replies remain future work.

## 2026-06-07

- Started PR58 provider write request queue.
- Added shared provider write contracts, `ProviderWriteRequest` schema/migration, `PROVIDER_WRITE_REVIEW_ADAPTERS` allowlist parsing, API routes, Web BFF routes, and tests for low-risk human-reviewed write requests.
- Added `npm run verify:provider-write-requests` and connected it to the production static CI workflow and provider adapter docs.
- Observed the first PR58 verifier red run: production readiness, public API surface, launch runbook, and task plan were missing provider write request references.
- Updated production readiness, public API surface, launch runbook, task plan, and progress notes so PR58 remains clearly review-only: no provider API calls, no provider writes, no customer-visible replies, no raw payload storage, and no browser operator-key exposure.
- Addressed PR58 safety review findings: `ProviderWriteRequest` now persists `idempotencyKeyHash` instead of the raw caller idempotency key, direct `POST /v2/provider-writes/request` requires an operator API key instead of insecure header fallback, and the Web BFF rejects provider write responses/lists unless they remain `networkExecution=not_started`, `providerMutationExecuted=false`, `customerVisibleMessageSent=false`, and human-reviewed.
- Final PR58 verification passed locally and was committed as `1cb116d feat(pr58): add provider write request queue`. Push is still blocked until GitHub OAuth is reauthorized with `workflow` scope because the branch includes the PR55 workflow file.
- Started PR59 provider write approval state machine. Scope: approve/reject transitions for queued provider write requests, two-person review, admin-only API/BFF routes, sanitized approval evidence, audit records, docs, and a static verifier. Approval still must not execute provider network writes or send customer-visible messages.
- Added PR59 red-green coverage and implementation for approve/reject state transitions, self-approval blocking, controlled reason-code payloads, sanitized review fingerprints, `payloadEscrowStatus=not_stored`, admin-only API routes, and Web BFF approve/reject routes.
- Added `npm run verify:provider-write-approval-state` plus static CI, production launch, public API surface, provider docs, production readiness, launch runbook, task plan, and progress references.
- Addressed PR59 review feedback: Web BFF provider write creation now parses `ProviderWriteRequestSchema` and rebuilds a controlled body before proxying, so browser-supplied `tenantId`/`operatorId` are not forwarded across the BFF boundary. Added a negative verifier test so missing PR59 approval route or CI wiring makes `verify:provider-write-approval-state` fail.
- Final PR59 verification passed locally: Prisma generate/migrate deploy, API tests, Web tests, PR59 verifier and verifier test, provider write request verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. API test and lint had known parallel Codex sandbox cwd failures, then passed when rerun alone from `G:\vibe-coding\smart-cs-agent`.
- PR59 is still decision-only: no provider write executor, no secure payload decrypt-on-execution, no real Taobao/Douyin writes, and no customer-visible message send path has been enabled.
- Committed PR59 locally as `33cc55f feat(pr59): add provider write approval state machine`. Push remains blocked until GitHub OAuth is reauthorized with `workflow` scope.
- Started PR60 provider write execution attempt safety. Scope: no-network `ProviderWriteExecutionAttempt` records, admin-only execution-attempt API/BFF route, default-on `PROVIDER_WRITE_EXECUTION_KILL_SWITCH`, idempotency, dry-run recording only when explicitly enabled, audit records, docs, and static verifier wiring.
- Added PR60 shared contracts, Prisma model/migration, API service/controller flow, Web BFF execution-attempt route, API/Web tests, config parsing, and the initial `verify:provider-write-execution-attempts` static gate. PR60 still does not call provider APIs, open payload escrow, execute provider writes, or send customer-visible messages.
- Addressed PR60 review findings: `payloadEscrowStatus=not_stored` is now the explicit dry-run safety precondition, execution-attempt idempotency is scoped to `tenantId + providerWriteRequestId + idempotencyKeyHash`, request/attempt fingerprints bind approval and escrow state, `payloadEscrowOpened=false` is persisted, the verifier rejects provider write calls or credential/decrypt access across execution helpers, and the Web BFF fails closed if an upstream execution-attempt response includes raw/provider/secret fields.
- Final PR60 verification passed locally: Prisma generate/migrate deploy, API tests, Web tests, PR60 verifier tests, PR60 verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. Lint had the known broad-parallel Codex sandbox cwd false failure, then passed when rerun alone from `G:\vibe-coding\smart-cs-agent`.
- Started PR61 provider write execution attempt invariants and visibility. Scope: PostgreSQL check constraints for no-network/no-escrow/customer-invisible execution attempts, shared sanitized list contract, admin-only API/BFF list routes, strict BFF upstream rejection, docs, static CI wiring, and production launch verifier wiring.
- Added the initial PR61 migration, shared `ProviderWriteExecutionAttemptListItem` contract, API service/controller list path, Web BFF route, API/Web tests, and `verify:provider-write-execution-attempt-visibility` static gate. PR61 still does not call provider APIs, read credentials, open payload escrow, execute provider writes, or send customer-visible replies.
- Addressed PR61 read-only review feedback by explicitly blocking `operatorVisibleResult` from provider write execution-attempt list visibility in the shared schema test, BFF unsafe-field guard, and PR61 verifier negative fixture.
- Final PR61 verification passed locally: Prisma generate/migrate deploy, API tests, Web tests, PR61 verifier tests, PR61 verifier, PR60 execution-attempt verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. The all-script run reported 107 pass / 1 skipped; the skipped test is the existing Windows symlink-permission case.

## 2026-06-08

- Started PR62 provider write payload escrow boundary. Scope: default-off `PROVIDER_WRITE_PAYLOAD_ESCROW_MODE`, request-only sealed metadata fingerprints, review preservation, execution-attempt blocking for sealed requests, no provider calls, no credential/decrypt access, no escrow opening, no customer-visible replies, docs, and static verifier wiring.
- Added request-only provider write payload escrow metadata, default-off config parsing, review preservation, sealed-request execution blocking, sanitized Web BFF status parsing, PR62 docs, and static verifier wiring.
- Addressed PR62 read-only review feedback by adding `ProviderWriteRequest_payload_escrow_consistency_chk`, so the database rejects mismatched request escrow status/mode/envelope/timestamp combinations instead of relying only on runtime writes.
- Final PR62 verification passed locally: Prisma generate/migrate deploy, API tests, Web tests, PR62 verifier tests, PR62 verifier, PR60/PR61 provider write safety verifiers, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. PR62 still does not execute provider writes, open/decrypt escrow, read provider credentials, store raw provider/customer payloads, or send customer-visible messages.
- Started PR63 provider write dry-run rehearsal evidence gate. Scope: sanitized `smart-cs-agent.provider-write-dry-run-rehearsal.v1` evidence under `provider-write-dry-run-rehearsal-artifacts/`, safe env-mode verification, no provider API calls, no provider writes, no credential/decrypt access, no payload escrow opening, no raw provider/customer payloads, no raw idempotency keys, docs, static CI, and production launch wiring.
- Added PR63 dry-run verifier tests and implementation, wired static CI/launch/docs, and fixed a launch-order review finding so `verify:provider-write-dry-run-rehearsal:safe` must run before `verify:production-provider-write-approval:safe`.
- Final PR63 verification passed locally: API tests, Web tests, PR63 verifier tests, production-launch order test, PR63/PR62/PR57/static-CI/launch verifiers, typecheck, lint, build, all script tests, and `git diff --check`. Independent reviews found no blocking issues; one P3 launch-test hardening item was fixed. PR63 still does not call provider APIs, execute provider writes, read credentials, open/decrypt escrow, store raw provider/customer payloads, or send customer-visible messages.
- Started PR64 provider write live executor startup guard. Scope: default-off `PROVIDER_WRITE_LIVE_EXECUTOR_ENABLED`, dry-run and approval evidence hashes, production startup fail-closed checks, sealed metadata readiness, review allowlists, credential ref requirements, no provider calls, no credential material reads, no escrow opening, no customer-visible replies, docs, and static verifier wiring.
- Final PR64 verification passed locally: API tests, Web tests, PR64 verifier tests, PR64/PR63/static-CI/launch verifiers, typecheck, lint, build, all script tests, and `git diff --check`. Read-only security and launch/static CI reviews found no P0/P1/P2/P3 findings. PR64 still does not call provider APIs, execute provider writes, read credential material, open/decrypt payload escrow, store raw provider/customer payloads, or send customer-visible replies.
- Started PR65 provider write live executor control plane. Scope: shared safe status contract, config status helper, admin-only API/BFF status routes, no evidence-hash or credential-ref exposure, no provider calls, no credential material reads, no escrow opening, no customer-visible replies, docs, static verifier wiring, and launch gate wiring.
- Addressed PR65 security review feedback by moving runtime status visibility to an `ApiConfigService` startup snapshot. `GET /v2/provider-writes/live-executor/status` no longer re-reads evidence hash env values or parses credential refs on the request path; it returns only the sanitized booleans, counts, gate names, and no-network invariants captured at startup.
- Final PR65 verification passed locally: API tests, Web tests, PR65 verifier syntax/test/run, PR64 startup guard verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. Read-only security review P1 and launch/static CI review P3 were both fixed. PR65 still does not call provider APIs, execute provider writes, read credential material, open/decrypt payload escrow, store raw provider/customer payloads, or send customer-visible replies.
- Started PR66 provider write kill switch control plane. Scope: shared safe status/update contracts, `ProviderWriteKillSwitchEvent` persistence, admin-only API/BFF status/update routes, persisted emergency-stop execution blocking, safe audit records, no raw idempotency keys, no provider calls, no credential material reads, no escrow opening, no customer-visible replies, docs, static verifier wiring, and launch gate wiring.
- Final PR66 verification passed locally: Prisma generate, API tests, Web tests, PR66 verifier syntax/test/run, PR60 execution-attempt verifier, PR64 startup guard verifier, PR65 control-plane verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. The first broad parallel API test hit the known Codex sandbox cwd / `experimentalDecorators` false failure, then passed when rerun alone from `G:\vibe-coding\smart-cs-agent`. PR66 still does not call provider APIs, execute provider writes, read credential material, open/decrypt payload escrow, store raw provider/customer payloads, expose raw idempotency keys, or send customer-visible replies.
- Started PR67 provider write kill switch rehearsal evidence gate. Scope: sanitized `smart-cs-agent.provider-write-kill-switch-rehearsal.v1` evidence under `provider-write-kill-switch-rehearsal-artifacts/`, safe env-mode verification, emergency-stop engage/block/release proof, no provider API calls, no provider writes, no credential/decrypt access, no payload escrow opening, no raw provider/customer payloads, no raw idempotency keys, docs, static CI, provider write approval, and production launch wiring.
- Final PR67 verification passed locally: API tests, Web tests, PR67 verifier syntax/test/run, production provider write approval verifier, production static CI verifier, production launch verifier, production launch order test, typecheck, lint, build, all script tests, and `git diff --check`. A read-only verifier review found no P0; its P1 findings were fixed by requiring exact workflow `uses:` action allowlisting and rejecting placeholder/repeated-character artifact hashes in kill-switch rehearsal evidence. PR67 remains evidence-only/no-network: no provider API calls, no provider writes, no credential material reads, no payload escrow opening/decrypting, no raw provider/customer payload storage, no raw idempotency-key output, and no customer-visible replies have been enabled.
- Started PR68 provider write live pilot preflight gate. Scope: sanitized `smart-cs-agent.provider-write-live-pilot-preflight.v1` evidence under `provider-write-live-pilot-preflight-artifacts/`, safe env-mode verification, single-merchant/single-channel/low-risk first pilot limits, operator coverage, rollback, observability, artifact bindings, docs, static CI, provider write approval, production launch ordering, and no provider API calls, no provider writes, no credential/decrypt access, no payload escrow opening, no raw provider/customer payloads, no raw idempotency keys, and no customer-visible replies.
- Final PR68 verification passed locally: API tests, Web tests, PR68 verifier syntax/test/run, production static CI verifier, production launch verifier, production launch order test, typecheck, lint, build, all script tests, and `git diff --check`. A read-only verifier review found no blocking issues; its optional schema-consistency finding was fixed by requiring `providerWriteKillSwitchControlPlaneSha256` alongside the kill-switch control-plane pass boolean. PR68 remains evidence-only/no-network: no provider API calls, no provider writes, no credential material reads, no payload escrow opening/decrypting, no raw provider/customer payload storage, no raw idempotency-key output, and no customer-visible replies have been enabled.
- Started PR69 provider write live pilot run ledger gate. Scope: sanitized `smart-cs-agent.provider-write-live-pilot-run-ledger.v1` evidence under `provider-write-live-pilot-run-ledger-artifacts/`, safe env-mode verification after a bounded pilot window closes, consistent run totals, all-runs-reviewed proof, failure/rollback closeout, audit and artifact bindings, docs, static CI, production launch wiring, and no verifier-side provider API calls, no verifier-side provider writes, no credential/decrypt access, no payload escrow opening, no raw provider/customer payloads, no raw idempotency keys, and no customer-visible replies.
- Addressed PR69 read-only review findings by requiring at least one run record when ledger evidence is required, requiring every run timestamp to stay inside the declared pilot window, and requiring rollback verification when a failed run already mutated provider state.
- Final PR69 verification passed locally: API tests, Web tests, PR69 verifier syntax/test/run, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. PR69 remains post-window evidence-only/no-network: no verifier-side provider API calls, no verifier-side provider writes, no credential material reads, no payload escrow opening/decrypting, no raw provider/customer payload storage, no raw idempotency-key output, and no customer-visible replies have been enabled.
- Started PR70 provider write live pilot run ledger draft export. Scope: admin-only `smart-cs-agent.provider-write-live-pilot-run-ledger-draft.v1` API/BFF export from existing provider write request and execution-attempt rows, bounded by tenant/channel/window, always `draftOnly=true`, `readyForSafeLedger=false`, and `canPassPr69SafeLedger=false`, with missing `artifact_bindings`, `live_provider_mutation_evidence`, and `manual_closeout_review`, plus `pilot_run_records` when no runs exist. PR70 must remain read-only/no-network: no provider API calls, no provider writes, no credential reads, no payload escrow opening/decrypting, no raw provider/customer payloads, no raw idempotency-key output, and no customer-visible replies.
- Addressed PR70 read-only review findings by failing closed instead of silently truncating draft windows over 50 records, enforcing required missing safe-ledger inputs in the shared schema, scoping request metadata lookup by channel, and constraining draft `policyReason` to a safe enum while sanitizing unknown stored values.
- Final PR70 verification passed locally: API tests, Web tests, PR70 verifier syntax/test/run, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. PR70 remains draft-only/read-only/no-network: no PR69 pass evidence, no `:safe` command, no provider API calls, no provider writes, no credential material reads, no payload escrow opening/decrypting, no raw provider/customer/idempotency data, and no customer-visible replies have been enabled.
- Started PR71 provider write manual closeout review gate. Scope: sanitized `smart-cs-agent.provider-write-manual-closeout-review.v1` evidence under `provider-write-manual-closeout-review-artifacts/`, safe env-mode verification, distinct reviewers, second review, `approved_for_safe_ledger`, consistent run counts, failed-run incident notes, rollback verification, no automatic customer-visible replies, artifact bindings, docs, static CI, PR69 binding, and production launch wiring. PR71 must remain evidence-only/no-network: no provider API calls, no provider writes, no credential reads, no payload escrow opening/decrypting, no raw provider/customer payloads, no raw idempotency-key output, and no customer-visible replies.
- Addressed PR71 read-only review findings by redacting unknown positional arguments, adding malformed JSON and unsafe safety flag tests, asserting second-review and closeout flag failures, and updating PR70/PR69 docs so `manual_closeout_review` and `providerWriteManualCloseoutReviewSha256` are explicit release-owner requirements.
- Final PR71 verification passed locally: API tests, Web tests, PR71 verifier syntax/test/run, PR69 ledger verifier, PR70 draft verifier, production static CI verifier, production launch verifier, typecheck, lint, build, all script tests, and `git diff --check`. PR71 remains manual closeout evidence-only/no-network: no provider API calls, no provider writes, no credential material reads, no payload escrow opening/decrypting, no raw provider/customer/idempotency data, and no customer-visible replies have been enabled.
- Started PR72 provider write safe ledger assembly gate. Scope: sanitized PR70 draft, PR71 manual closeout review, and PR69 final ledger evidence assembly under their artifact directories; byte-level SHA-256 recomputation; target/window/run-correlation checks; static CI and production launch wiring; docs and tracking. PR72 must remain assembly-only/no-network: no provider API calls, no provider writes, no credential reads, no production DB reads, no payload escrow opening/decrypting, no raw provider/customer payloads, no raw idempotency-key output, and no customer-visible replies.
- Added PR72 verifier tests and implementation for safe missing evidence, static no-evidence mode, matching sanitized assembly, broken draft/review hash bindings, audit/production launch hash mismatches, target and change-ticket mismatches, launch window mismatches, duplicate/unmatched run correlations, pass-shaped draft rejection, unsafe safety flags, sensitive evidence rejection, unsafe paths, and redacted unknown arguments.
- Final PR72 verification passed locally: PR72 verifier tests, PR72 static verifier, production static CI verifier, production launch verifier, all script tests, API tests, Web tests, typecheck, lint, build, and `git diff --check`. The all-script run reported 191 pass / 1 skipped; the skipped test is the existing Windows symlink-permission case. PR72 remains assembly-only/no-network and does not enable provider writes, credential reads, payload escrow opening, production DB reads, or customer-visible replies.
- Started PR73 provider write controlled expansion approval gate. Scope: sanitized controlled expansion approval evidence plus separate PR72 safe ledger assembly receipt, byte-level PR72 receipt SHA-256 recomputation, bounded transition from `single_merchant_pilot` to `controlled_multi_merchant`, merchant/action/limit controls, operator and rollback coverage, alerting, commercial readiness, static CI and production launch wiring, docs, and tracking. PR73 must remain approval-only/no-network: no provider API calls, no provider writes, no credential reads, no production DB reads, no payload escrow opening/decrypting, no raw provider/customer payloads, no raw idempotency-key output, and no customer-visible replies.
- Addressed PR73 read-only review findings by making safe mode require PR70 draft, PR71 manual closeout review, and PR69 final ledger source artifacts in addition to the PR72 safe ledger assembly receipt. The verifier now recomputes all source file SHA-256 values before accepting the assembly receipt bindings, rejects unsafe change ticket IDs, rejects common placeholder/weak SHA-256 values, and keeps static safety scans away from network, provider, database, credential, escrow, and customer-reply code paths.
- Final PR73 verification passed locally: PR73 verifier tests, PR73 static verifier, production static CI verifier, production launch verifier, all script tests, API tests, Web tests, typecheck, lint, and build. The all-script run reported 200 pass / 1 skipped; the skipped test is the existing Windows symlink-permission case. A broad parallel lint run hit the known Codex sandbox temp-cwd ESLint project-path false failure, then `npm.cmd run lint --workspaces --if-present -- --max-warnings=0` passed when rerun alone from `G:\vibe-coding\smart-cs-agent`. PR73 remains approval-only/no-network and does not enable provider writes, credential reads, production DB reads, payload escrow opening, or customer-visible replies.
- Started PR74 provider write controlled expansion preflight gate. Scope: sanitized `smart-cs-agent.provider-write-controlled-expansion-preflight.v1` launch-window evidence under `provider-write-controlled-expansion-preflight-artifacts/`, byte-level PR73 approval SHA-256 recomputation, bounded `controlled_multi_merchant` scope checks against PR73 approval, freeze-window and rollback controls, operator and support coverage, static CI and production launch wiring, docs, and tracking. PR74 must remain preflight-only/no-network: no provider API calls, no provider writes, no credential reads, no production DB reads, no payload escrow opening/decrypting, no raw provider/customer payloads, no raw idempotency-key output, and no customer-visible replies.
- Addressed PR74 review feedback by making safe mode require the PR73 safe-ledger assembly receipt plus PR70 draft, PR71 manual closeout review, and PR69 final ledger source artifacts. The verifier now recomputes the approval file SHA-256, assembly receipt SHA-256, and source artifact SHA-256 bindings before accepting a controlled expansion preflight window.
